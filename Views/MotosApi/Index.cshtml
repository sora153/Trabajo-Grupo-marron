@model Trabajo.DTO.MotosApiDTO

@{
    ViewData["Title"] = "TipoCambio";
}

<div class="container">
    <h1 class="text-center mb-4">Formulario de Tipo Cambio</h1>
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form asp-controller="MotosApi" asp-action="Motos" method="post">
                <div class="form-group">
                    <label asp-for="Model" class="control-label">Modelo</label>
                    <select asp-for="Model" class="form-control" ></select>
                </div>
                <div class="form-group">
                    <label asp-for="Make" class="control-label">Marca</label>
                    <select asp-for="Make" class="form-control" ></select>
                </div>
                <div class="form-group">
                    <label asp-for="Year" class="control-label">Año</label>
                    <select asp-for="Year" class="form-control" ></select>
                </div>                
                
            <br>
            <div class="form-group text-center">
                    <button type="submit" class="btn btn-danger btn-lg">Mostrar especificaciones</button>
                </div>
            <div>
        <a href="http://localhost:5208">volver al inicio</a>
    </div>
        </div>
    </div>
</div>